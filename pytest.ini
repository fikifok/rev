# pytest.ini
[pytest]
# testlerinizi tests/ içinde arar
testpaths = tests
# çalışma dizini olarak proje kökünü kullan
# (pytest’in Python path’ine proje kökünü ekler)
# not: bazı pytest sürümleri 'pythonpath' yerine 'addopts = --rootdir=.' gerekebilir.
addopts = --rootdir=.
pythonpath = .
